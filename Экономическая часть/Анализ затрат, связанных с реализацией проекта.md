#### Постоянные затраты (независящие от объёма выпускаемой продукции)

###### Зарплатный фонд сотрудников, работающих над реализацией проекта:
- Конструктор - 100 тысяч рублей в месяц
- Программист-электронщик - 45 тысяч рублей в месяц
- Начальник отдела - 350 тысяч рублей в месяц

---
#### Переменные затраты (зависящие от объёма выпускаемой продукции)

| Наименование                                                                                                  | Затраты          | Примечания                                   |
| ------------------------------------------------------------------------------------------------------------- | ---------------- | -------------------------------------------- |
| Конструкционная рама                                                                                          | 100 тысяч рублей | Ориентировочная стоимость, требует уточнений |
| [[Расчёт стоимости разрабатываемой части проекта\|Блок с инерциальными датчиками и управляющим контроллером]] | 40 тысяч рублей  | Разрабатываемая мною часть проекта           |
| Блок датчиков пройденного пути                                                                                | 10 тысяч рублей  | Ориентировочная стоимость, требует уточнений |
| Блок датчиков для измерения ширины колеи                                                                      | 10 тысяч рублей  | Ориентировочная стоимость, требует уточнений |
| Компьютер для сбора и обработки данных                                                                        | 50 тысяч рублей  | Зависит от выбранной модели                  |
| Аккумулятор                                                                                                   | 10 тысяч рублей  | Ориентировочная стоимость, требует уточнений |

```dataviewjs
// Получаем текущий файл
const page = dv.current()

// Ищем таблицы в содержимом файла
const content = await dv.io.load(page.file.path)
const tableRegex = /\|.*\|\n\|.*\|\n((?:\|.*\|\n)*)/g
let match
let total = 0

while ((match = tableRegex.exec(content)) !== null) {
    const rows = match[1].trim().split('\n')
    rows.forEach(row => {
        const cells = row.split('|').filter(cell => cell.trim() !== '')
        if (cells.length >= 2) {
            const cost = parseInt(cells[1].trim().replace(/\D/g, ''))
            if (!isNaN(cost)) {
                total += cost
            }
        }
    })
}

dv.paragraph(`**Общая сумма оборудования: ${total} тысяч рублей**`)
```

---

